{
  "name": "tkd-championship-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/obfuscate.js",
    "deploy": "npm run build && firebase deploy"
  },
  "devDependencies": {
    "javascript-obfuscator": "^4.1.1",
    "fs-extra": "^11.2.0"
  }
}
